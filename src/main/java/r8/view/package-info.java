/**
 * View controllers for user interface are organized in here in sub-packages.
 * Login view and Main view are the two parent view of the software in which all subviews are loaded.
 * Subviews of both parent view can be found under their corresponding packages.
 * For example when Main view is loaded, it stays active and all subview are loaded within it.
 * This gives the software it's single page app feel when navigating the UI.
 *
 * @author Aarni Pesonen
 */

package r8.view;